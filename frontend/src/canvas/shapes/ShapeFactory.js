/**
 * Create a line shape from fitted geometry.
 *
 * @param {object} geometry - Fitted line geometry.
 * @param {object} options - Future shape and layer options.
 * @returns {null}
 */
export function createLine(geometry, options) {
  void geometry;
  void options;

  // TODO: Create and configure a Fabric.js line object.
  return null;
}

/**
 * Create a rectangle shape from fitted geometry.
 *
 * @param {object} geometry - Fitted rectangle geometry.
 * @param {object} options - Future shape and layer options.
 * @returns {null}
 */
export function createRectangle(geometry, options) {
  void geometry;
  void options;

  // TODO: Create and configure a Fabric.js rectangle object.
  return null;
}

/**
 * Create a circle shape from fitted geometry.
 *
 * @param {object} geometry - Fitted circle geometry.
 * @param {object} options - Future shape and layer options.
 * @returns {null}
 */
export function createCircle(geometry, options) {
  void geometry;
  void options;

  // TODO: Create and configure a Fabric.js circle object.
  return null;
}

/**
 * Create an ellipse shape from fitted geometry.
 *
 * @param {object} geometry - Fitted ellipse geometry.
 * @param {object} options - Future shape and layer options.
 * @returns {null}
 */
export function createEllipse(geometry, options) {
  void geometry;
  void options;

  // TODO: Create and configure a Fabric.js ellipse object.
  return null;
}

/**
 * Create a polygon shape from fitted geometry.
 *
 * @param {object} geometry - Fitted polygon geometry.
 * @param {object} options - Future shape and layer options.
 * @returns {null}
 */
export function createPolygon(geometry, options) {
  void geometry;
  void options;

  // TODO: Create and configure a Fabric.js polygon object.
  return null;
}

/**
 * Create an arrow shape from fitted geometry.
 *
 * @param {object} geometry - Fitted arrow geometry.
 * @param {object} options - Future shape and layer options.
 * @returns {null}
 */
export function createArrow(geometry, options) {
  void geometry;
  void options;

  // TODO: Create and configure the Fabric.js objects that compose an arrow.
  return null;
}

/**
 * Create a speech bubble shape from fitted geometry.
 *
 * @param {object} geometry - Fitted speech bubble geometry.
 * @param {object} options - Future shape and layer options.
 * @returns {null}
 */
export function createSpeechBubble(geometry, options) {
  void geometry;
  void options;

  // TODO: Create and configure the Fabric.js objects that compose a speech bubble.
  return null;
}

/**
 * Create a star shape from fitted geometry.
 *
 * @param {object} geometry - Fitted star geometry.
 * @param {object} options - Future shape and layer options.
 * @returns {null}
 */
export function createStar(geometry, options) {
  void geometry;
  void options;

  // TODO: Create and configure a Fabric.js star object.
  return null;
}
