[pytest]
# Make the `app` package importable without an app/__init__.py (namespace pkg).
pythonpath = .
testpaths = tests
# Let pytest-asyncio run `async def` tests without a per-test decorator.
asyncio_mode = auto
markers =
    integration: requires the full stack running on http://localhost:8000 (docker compose up)
